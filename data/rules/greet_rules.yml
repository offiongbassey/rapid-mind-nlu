# rules.yml
version: "3.1"
rules:
- rule: Say goodbye when the user says goodbye
  steps:
    - intent: goodbye
    - action: utter_goodbye
- rule: Ask for clarification if message is unclear
  steps:
    - intent: nlu_fallback
    - action: utter_ask_specific
